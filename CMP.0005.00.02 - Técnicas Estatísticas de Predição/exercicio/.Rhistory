setwd("~/Git/pos-datascience/CMP.0005.00.02 - Técnicas Estatísticas de Predição")
dados <- read.csv("Exercício Preparatório.csv", header = T, sep = ";", dec = ",")
View(dados)
View(dados)
summary(dados$salario)
print('oar')
# Questão 1: As medidas de tendência central das variáveis numéricas (mínimo, máximo, 1o Quartil, 3o Quartil, mediana e média)
summary(dados$salario)
summary(dados$idade)
summary(dados$stress)
## Amplitude
max(dados$salario)-min(dados$salario)
max(dados$idade)-min(dados$idade)
max(dados$stress)-min(dados$stress)
# Questão 1: As medidas de tendência central das variáveis numéricas (mínimo, máximo, 1o Quartil, 3o Quartil, mediana e média)
print(summary(dados$salario))
# Questão 1: As medidas de tendência central das variáveis numéricas (mínimo, máximo, 1o Quartil, 3o Quartil, mediana e média)
print('asdasd', summary(dados$salario))
cat('aaa')
cat('aaa', summary(dados$salario))
# Questão 1: As medidas de tendência central das variáveis numéricas (mínimo, máximo, 1o Quartil, 3o Quartil, mediana e média)
summary(dados$salario)
summary(dados$idade)
summary(dados$stress)
## Amplitude
max(dados$salario)-min(dados$salario)
max(dados$idade)-min(dados$idade)
max(dados$stress)-min(dados$stress)
## Desvio=Padrão
sd(dados$salario)
sd(dados$idade)
sd(dados$stress)
## Variância
var(dados$salario)
var(dados$idade)
var(dados$stress)
## Coeficiente de Variação
sd(dados$salario) / mean(dados$salario)
sd(dados$idade) / mean(dados$idade)
sd(dados$stress) / mean(dados$stress)
## Assimetria
library(fBasics)
skewness(dados$salario)
skewness(dados$idade)
skewness(dados$stress)
## Curtose
kurtosis(dados$salario)
kurtosis(dados$idade)
kurtosis(dados$stress)
View(dados)
dados$sexo
dados$sexo = 1
dados$sexo == 1
# Carregando os dados
dados <- read.csv("Exercício Preparatório.csv", header = T, sep = ";", dec = ",")
dados
dados$sexo
# Carregando os df
df <- read.csv("Exercício Preparatório.csv", header = T, sep = ";", dec = ",")
-
subset(df, (sexo == 1))
subset.matrix(df, (sexo == 1))
View(df)
subset(df, (sexo == 1))
subset(df, (sexo == 1))
# Carregando os df
df <- read.csv("Exercício Preparatório.csv", header = T, sep = ";", dec = ",")
# Questão 3: Calcule a média e a mediana do salário para homens e mulheres separadamente.
df
# Questão 3: Calcule a média e a mediana do salário para homens e mulheres separadamente.
subset(df, (sexo == 1))
# Questão 3: Calcule a média e a mediana do salário para homens e mulheres separadamente.
df_homens = subset(df, (sexo == 0))
df_mulheres = subset(df, (sexo == 1))
mean(df_homens$salario)
mean(df_mulheres$salario)
median(df_homens$salario)
median(df_mulheres$salario)
# Questão 4: Calcule o desvio-padrão e o coeficiente de variação para homens e mulheres separadamente e verifique se existe diferença nas variações.
## Homens
sd(df_homens$salario)
sd(df_homens$idade)
sd(df_homens$stress)
sd(df_homens$salario) / mean(df_homens$salario)
sd(df_homens$idade) / mean(df_homens$idade)
sd(df_mulheres$stress) / mean(df_homens$stress)
sd(df_homens$salario) / mean(df_homens$salario)
sd(df_homens$idade) / mean(df_homens$idade)
sd(df_homens$stress) / mean(df_homens$stress)
## Mulheres
sd(df_mulheres$salario)
sd(df_mulheres$idade)
sd(df_mulheres$stress)
sd(df_mulheres$salario) / mean(df_mulheres$salario)
sd(df_mulheres$idade) / mean(df_mulheres$idade)
sd(df_mulheres$stress) / mean(df_mulheres$stress)
View(df)
# Questão 5: Calcule a média e a mediana do salário para casado e solteiros separadamente.
df_solteiros = subset(df, (est_civil == 0))
df_casados = subset(df, (est_civil == 1))
mean(df_solteiros$salario)
median(df_solteiros$salario)
mean(df_casados$salario)
median(df_casados$salario)
# Questão 6: Calcule a média e a mediana do nível de stress para homens e mulheres separadamente
mean(df_homens$stress)
median(df_homens$stress)
mean(df_mulheres$stress)
median(df_mulheres$stress)
mean(df_solteiros$salario)
median(df_solteiros$salario)
mean(df_casados$salario)
median(df_casados$salario)
# Questão 7: Calcule a média e a mediana do nível de stress para casado e solteiros separadamente.
mean(df_solteiros$stress)
median(df_solteiros$stress)
mean(df_casados$stress)
median(df_casados$stress)
summary(df$salario)
summary(df$idade)
summary(df$stress)
skewness(df$salario)
skewness(df$idade)
skewness(df$stress)
## Curtose
kurtosis(df$salario)
kurtosis(df$idade)
kurtosis(df$stress)
## Curtose
kurtosis(df$salario);
mean(df_homens$salario)
mean(df_mulheres$salario)
median(df_homens$salario)
median(df_mulheres$salario)
# Questão 4: Calcule o desvio-padrão e o coeficiente de variação para homens e mulheres separadamente e verifique se existe diferença nas variações.
## Homens
### Desvio Padrão
sd(df_homens$salario)
sd(df_homens$idade)
sd(df_homens$stress)
### Coeficiente de Variação
sd(df_homens$salario) / mean(df_homens$salario)
sd(df_homens$idade) / mean(df_homens$idade)
sd(df_homens$stress) / mean(df_homens$stress)
## Mulheres
### Desvio Padrão
sd(df_mulheres$salario)
sd(df_mulheres$idade)
sd(df_mulheres$stress)
# Questão 8: Faça um gráfico de dispersão que compare o nível de stress com o salário do entrevistado.
df$stress
# Questão 8: Faça um gráfico de dispersão que compare o nível de stress com o salário do entrevistado.
stress <- df$stress
salario <- df$salario
plot(stress~salario)
model <- lm(stress~salario)
plot(stress~salario)
abline(model, col="red")
model <- lm(stress~salario)
abline(model, col="red")
plot(stress~salario)
plot(stress~salario)
model <- lm(stress~salario)
abline(model, col="red")
# Questão 9: Faça um gráfico de dispersão que compare o nível de stress com a idade do entrevistado.
stress <- df$stress
idade <- df$idade
model <- lm(stress~idade)
# Questão 9: Faça um gráfico de dispersão que compare o nível de stress com a idade do entrevistado.
stress <- df$stress
idade <- df$idade
plot(stress~idade)
model <- lm(stress~idade)
abline(model, col="red")
boxplot(df$salario)
boxplot(df$idade)
boxplot(df$stress)
boxplot(df$idade)
boxplot(df$salario)
boxplot(df$salario)
boxplot(df$idade)
boxplot(df$stress)
plot(stress~idade)
model <- lm(stress~idade)
abline(model, col="red")
## Salário
max(df$salario)-min(df$salario)
sd(df$salario)
var(df$salario)
sd(df$salario) / mean(df$salario)
skewness(df$salario)
kurtosis(df$salario)
## Salário
max(df$salario)-min(df$salario)
sd(df$salario)
var(df$salario)
sd(df$salario) / mean(df$salario)
skewness(df$salario)
kurtosis(df$salario)
## Idade
max(df$idade)-min(df$idade)
sd(df$idade)
var(df$idade)
sd(df$idade) / mean(df$idade)
skewness(df$idade)
kurtosis(df$idade)
## Stress
max(df$stress)-min(df$stress)
sd(df$stress)
var(df$stress)
sd(df$stress) / mean(df$stress)
skewness(df$stress)
kurtosis(df$stress)
kurtosis(df$salario)
# Questão 4: Calcule o desvio-padrão e o coeficiente de variação do salário para homens e mulheres separadamente e verifique se existe diferença nas variações.
### Desvio Padrão
sd(df_homens$salario)
sd(df_mulheres$salario)
### Coeficiente de Variação
sd(df_homens$salario) / mean(df_homens$salario)
sd(df_mulheres$salario) / mean(df_mulheres$salario)
plot(stress~salario)
model <- lm(stress~salario)
abline(model, col="red")
plot(stress~salario, xlab="Salário", ylab="Stress")
model <- lm(stress~salario)
abline(model, col="red")
plot(stress~salario, xlab="Salário", ylab="Stress", main="Stress X Salário")
model <- lm(stress~salario)
abline(model, col="red")
plot(stress~salario, xlab="Salário", ylab="Stress", main="Stress X Salário")
model <- lm(stress~salario)
abline(model, col="red")
abline(h=mean(df$stress), col="green")
abline(v=mean(df$salario), col="green")
plot(stress~idade, xlab="Idade", ylab="Stress", main="Stress X Idade")
model <- lm(stress~idade)
abline(model, col="red")
abline(h=mean(df$stress), col="green")
abline(v=mean(df$idade), col="green")
## Extra
boxplot(df$salario ~ df$sexo)
## Extra
boxplot(df$salario ~ df$sexo)
boxplot(df$salario ~ df$stress)
# Para o caso das mulheres
media_mulher <- mean(df_mulheres$salario)
dp_mulher <- sd(df_mulheres$salario)
q3 <- quantile(dados$salario, probs=0.75)
# Probabilidade de uma mulher receber mais queo Q3
# P(Sal > Q3) = 1 - P(Sal <= Q3)
1 - pnorm(Q3, mean = media_mulher, sd = dp_mulher)
# Probabilidade de uma mulher receber mais queo Q3
# P(Sal > Q3) = 1 - P(Sal <= Q3)
1 - pnorm(q3, mean = media_mulher, sd = dp_mulher)
# Para o caso dos homens
media_homem <= mean(df_homens$salario)
dp_homem <= sd(df_homens$salario)
q3 <- quantile(dados$salario, probs=0.75)
1 - pnorm(q3, mean = media_homem, sd = dp_homem)
# Para o caso dos homens
media_homem <- mean(df_homens$salario)
dp_homem <- sd(df_homens$salario)
q3 <- quantile(dados$salario, probs=0.75)
1 - pnorm(q3, mean = media_homem, sd = dp_homem)
